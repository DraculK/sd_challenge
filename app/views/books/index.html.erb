<body class="index-background">
    <% @books.each do |book| %>
    <h2><%= link_to book.title, show_book_path(book) %></h2>
    <%end%>

    <%= link_to "Adicionar novo livro", new_book_path%>
</body>